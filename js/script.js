new WOW().init();

var typed = new Typed(".text", {
  strings: ["FullStack Engineer", "Frontend Designer", "BackEnd Development"],
  typeSpeed: 100,
  backSpeed: 100,
  backDelay: 1000,
  loop: true,
});
